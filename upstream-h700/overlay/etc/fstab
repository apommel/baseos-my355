# Informational only — rcS performs all mounts explicitly (fast path).
# /            ext4   ro,noatime   (mmcblk0p5, the active A/B rootfs slot)
# /data        ext4   rw,noatime   (mmcblk0p6 "UDISK", persistent state)
# /mnt/sdcard  vfat   rw           (mmcblk1p1 TF2 if present, else mmcblk0p7)
