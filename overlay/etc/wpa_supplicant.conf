# Seeded into /userdata/cfg/ by S36load_wifi_modules, as stock does from here.
# ctrl_interface is the socket NextUI's wpa_cli calls use; update_config lets
# wpa_supplicant save networks back.
ctrl_interface=/var/run/wpa_supplicant
update_config=1
